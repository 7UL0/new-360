"use client";
import React from "react";

const Form = () => {
  type UserFormProps = UserData & {
    updateFields: (fields: Partial<UserData>) => void
  }

  return (
    <>
    </>
  );
};

export default Form;
